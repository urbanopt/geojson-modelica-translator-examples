# Run `pip install -r requirements.txt` or `pip install -U --upgrade-strategy eager -r requirements.txt`

# Not sure why requests isn't installed with GMT, and it needs to be installed before the rest of
# this file runs, so complete it outside of this file.
requests==2.24.0

# GMT
# -e git+https://github.com/urbanopt/geojson-modelica-translator.git@develop#egg=geojson_modelica_translator
-e file:../geojson-modelica-translator#egg=geojson_modelica_translator

# Test and documentation
nose==1.3.7
sphinx==3.2.1
sphinx_rtd_theme==0.5.0
pre-commit==2.7.1
pytest==6.1.0
pytest-cov==2.10.1
python-coveralls==2.9.3
autopep8==1.5
flake8==3.8.3
tox==3.20.0
